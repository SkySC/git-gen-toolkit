[tool.coverage.run]
branch = true
source = [ "llm_connection" ]

[tool.coverage.report]
exclude_lines = ['if TYPE_CHECKING:']
show_missing = true

[tool.pytest.ini_options]
addopts = """
 --cov
 --cov-report html:'../coverage/llm-connection/html'
 --cov-report xml:'../coverage/llm-connection/coverage.xml'
 --html='../reports/llm-connection/unittests/html/index.html'
 --junitxml='../reports/llm-connection/unittests/junit.xml'
"""

[project]
name = "llm-connection"
version = "1.0.0"
description = "Automatically generated by Nx."
requires-python = ">=3.9,<4"
readme = 'README.md'
dependencies = [
    "requests>=2.31.0",
    "types-requests>=2.31.0.20231231",
    "toolkit-utils>=1.0.0"
]

[tool.hatch.build.targets.wheel]
packages = ["llm_connection"]

[tool.hatch.metadata]
allow-direct-references = true

[dependency-groups]
dev = [
  "autopep8>=2.3.1",
  "ruff>=0.8.2",
  "pytest>=8.3.4",
  "pytest-sugar>=1.0.0",
  "pytest-cov>=6.0.0",
  "pytest-html>=4.1.1",
  "black>=23.3.0",
  "flake8>=5.0.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
exclude = [
  ".ruff_cache",
  ".svn",
  ".tox",
  ".venv",
  "dist",
]

line-length = 88
indent-width = 4

[tool.ruff.lint]
select = [
  # pycodestyle
  "E",
  # Pyflakes
  "F",
  # pyupgrade
  "UP",
  # flake8-bugbear
  "B",
  # flake8-simplify
  "SIM",
  # isort
  "I",
]
ignore = []

fixable = ["ALL"]
unfixable = []

[tool.black]
line-length = 80
preview = true
enable-unstable-feature = ["string_processing"]
